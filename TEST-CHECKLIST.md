# Test Checklist — SDG-TERM

## Shell Configuration
- [ ] Ghostty config applied — 50% opacity, dark theme active
- [ ] Zsh config loads without errors (watch for error messages on shell start)
- [ ] Powerlevel10k prompt shows SDG-OS style
- [ ] `sdgfetch` runs automatically on terminal start

## Aliases
- [ ] `tldrtui` resolves
- [ ] `sdgdocs` resolves
- [ ] `sdgtip` / `tipme` / `alltips` resolve
- [ ] `git-projects` resolves to `/usr/bin/` file
- [ ] `mangoconf` resolves to `/usr/bin/` file
- [ ] `documentation` resolves to `/usr/bin/` file
- [ ] `tldrtui` resolves to `/usr/bin/` file
- [ ] `helptui` does not exist (removed)
- [ ] Arch-specific aliases (from `.distros.zsh`) don't error on non-Arch systems
- [ ] `update` alias works on Arch, provides alternatives on other distros
